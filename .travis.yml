language: ruby
rvm:
  - 1.9.3

before_script:
  - "cp config/database.sample.yml config/database.yml"
  - "psql -c 'create role imagine SUPERUSER LOGIN;' postgres"
  - "psql -c 'create database imagine_test;' -U imagine postgres"
  - "sh -e /etc/init.d/xvfb start"

env:
  - DISPLAY=:99.0

script: "bundle exec rake db:migrate && bundle exec rspec spec && bundle exec cucumber"

branches:
  only:
    - master
